import { useQuery } from "@tanstack/react-query"; import { apiGet } from "../api/client";
export function ModuleManager(){const q=useQuery({queryKey:["modules"],queryFn:()=>apiGet<any>("/api/modules")});return <div className="space-y-5"><div><h2 className="text-3xl font-black">Module Manager</h2><p className="text-slate-400">AI-native tactical module SDK foundation.</p></div><div className="grid grid-cols-2 gap-4"><div className="card p-5"><h3 className="font-bold">SDK Capabilities</h3><pre className="text-xs mt-3 bg-black/30 p-3 rounded-xl overflow-auto">{JSON.stringify(q.data?.sdk,null,2)}</pre></div><div className="card p-5"><h3 className="font-bold">Installed Modules</h3>{q.data?.modules?.length?q.data.modules.map((m:any)=><div key={m.id}>{m.name}</div>):<p className="text-slate-400 mt-3">No modules installed.</p>}</div></div></div>}
