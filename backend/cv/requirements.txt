# Ultralytics pulls PyTorch. This project refuses CPU training: install a
# Windows CUDA PyTorch wheel, add torch-directml for Windows DirectML, or use
# the WSL ROCm runtime via tools/cv-wsl.ps1.
ultralytics>=8.3,<9
onnx>=1.16,<2
onnxruntime-directml>=1.20,<2
